from fastapi import APIRouter

# Módulo de AI desativado para a versão de produção do AmazonTech DevLab.
# Este router existe apenas para compatibilidade, mas não registra nenhuma rota.

router = APIRouter()